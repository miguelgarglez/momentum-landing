---
const title = 'Momentum — Time tracking for makers';
const description =
  'Track project time without friction. Built for focused developers and creators. Includes a Raycast extension for instant logging.';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <style>
      :root {
        --bg: #0b1020;
        --card: #141b34;
        --text: #edf2ff;
        --muted: #b6c0e4;
        --accent: #7c9cff;
        --line: #273362;
      }
      * { box-sizing: border-box; }
      html { scroll-behavior: smooth; }
      body {
        margin: 0;
        font-family: Inter, ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif;
        background: radial-gradient(circle at top, #151f3e 0%, var(--bg) 55%);
        color: var(--text);
      }
      .container { max-width: 980px; margin: 0 auto; padding: 2rem 1rem 4rem; }
      .hero {
        text-align: center;
        padding: 3.5rem 1rem 2rem;
      }
      h1 { font-size: clamp(2rem, 6vw, 3.6rem); margin: 0 0 0.8rem; }
      p { color: var(--muted); line-height: 1.6; }
      .lead { max-width: 720px; margin: 0 auto 1.5rem; }
      .actions { display: flex; gap: 0.8rem; justify-content: center; flex-wrap: wrap; }
      .btn {
        text-decoration: none;
        padding: 0.75rem 1.1rem;
        border-radius: 12px;
        font-weight: 600;
        border: 1px solid transparent;
        cursor: pointer;
      }
      .btn.primary { background: var(--accent); color: #0b1020; }
      .btn.ghost { border-color: #36467f; color: var(--text); }
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 1rem;
        margin-top: 2rem;
      }
      .card {
        background: color-mix(in oklab, var(--card), black 8%);
        border: 1px solid var(--line);
        border-radius: 14px;
        padding: 1rem;
      }
      h2 { margin-top: 0; font-size: 1.05rem; }
      .section { margin-top: 2.2rem; }

      .screenshots {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 1rem;
      }
      .shot {
        border: 1px solid var(--line);
        border-radius: 14px;
        overflow: hidden;
        background: #0f1530;
      }
      .shot .ph {
        aspect-ratio: 16 / 10;
        background: linear-gradient(135deg, #1c2750, #0e1430);
        display: grid;
        place-items: center;
        color: #9cb0ff;
        font-weight: 600;
      }
      .shot p { margin: 0; padding: 0.8rem; }

      .waitlist {
        text-align: center;
      }
      .waitlist form {
        margin-top: 1rem;
        display: flex;
        gap: 0.6rem;
        justify-content: center;
        flex-wrap: wrap;
      }
      input[type="email"] {
        min-width: 280px;
        max-width: 420px;
        width: 100%;
        border: 1px solid #3a4b87;
        background: #0c1230;
        color: var(--text);
        padding: 0.75rem 0.9rem;
        border-radius: 12px;
      }

      details {
        border: 1px solid var(--line);
        border-radius: 12px;
        padding: 0.8rem 1rem;
        background: #0f1530;
      }
      details + details { margin-top: 0.7rem; }
      summary {
        cursor: pointer;
        font-weight: 600;
      }

      .final-cta {
        text-align: center;
      }

      footer {
        margin-top: 3rem;
        color: #98a6d8;
        font-size: 0.95rem;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <main class="container">
      <section class="hero" id="top">
        <p style="margin:0;opacity:.9;">Momentum for macOS</p>
        <h1>Track your real project time, not guesses.</h1>
        <p class="lead">
          Momentum helps you log focused work sessions in seconds, organize by project,
          and review where your time actually goes. Plus, log instantly with the Raycast extension.
        </p>
        <div class="actions">
          <a class="btn primary" href="#waitlist">Join waitlist</a>
          <a class="btn ghost" href="#how">See how it works</a>
        </div>
      </section>

      <section class="grid section" id="how">
        <article class="card">
          <h2>Fast capture</h2>
          <p>Start/stop logs quickly so tracking never interrupts your flow.</p>
        </article>
        <article class="card">
          <h2>Project-first</h2>
          <p>Group sessions by project to understand effort and priorities clearly.</p>
        </article>
        <article class="card">
          <h2>Raycast extension</h2>
          <p>Capture time from anywhere with quick commands, no context switching.</p>
        </article>
      </section>

      <section class="section">
        <h2>Screenshots</h2>
        <div class="screenshots">
          <article class="shot">
            <div class="ph">Dashboard screenshot</div>
            <p>See project totals and trend of focused sessions.</p>
          </article>
          <article class="shot">
            <div class="ph">Session logging screenshot</div>
            <p>Log time in seconds and keep momentum in deep work blocks.</p>
          </article>
          <article class="shot">
            <div class="ph">Raycast extension screenshot</div>
            <p>Capture from anywhere without leaving your current app.</p>
          </article>
        </div>
      </section>

      <section class="section card waitlist" id="waitlist">
        <h2>Get early access</h2>
        <p>Be the first to try Momentum and get launch updates.</p>
        <form action="https://formspree.io/f/your-form-id" method="POST">
          <input type="email" name="email" placeholder="you@domain.com" required />
          <button class="btn primary" type="submit">Join waitlist</button>
        </form>
        <p style="font-size:.9rem;margin-top:.8rem;">Tip: replace the Formspree endpoint with your own.</p>
      </section>

      <section class="section" id="faq">
        <h2>FAQ</h2>
        <details>
          <summary>When will Momentum launch?</summary>
          <p>I’m polishing the final version and collecting early feedback now.</p>
        </details>
        <details>
          <summary>Is it only for developers?</summary>
          <p>No. It’s optimized for makers, but anyone doing project-based work can use it.</p>
        </details>
        <details>
          <summary>Will there be a free trial?</summary>
          <p>Most likely yes. Early users will get launch details first.</p>
        </details>
      </section>

      <section class="section card final-cta">
        <h2>Ready to track your real output?</h2>
        <p>Skip spreadsheets. Keep your focus. Ship more with clear time data.</p>
        <a class="btn primary" href="#waitlist">Join waitlist</a>
      </section>

      <footer>
        Built by Miguel · Momentum
      </footer>
    </main>
  </body>
</html>
