---
const title = 'Momentum — Time tracking for makers';
const description =
  'Track project time without friction. Built for focused developers and creators. Includes a Raycast extension for instant logging.';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <style>
      :root {
        --bg: #0b1020;
        --card: #141b34;
        --text: #edf2ff;
        --muted: #b6c0e4;
        --accent: #7c9cff;
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        font-family: Inter, ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif;
        background: radial-gradient(circle at top, #151f3e 0%, var(--bg) 55%);
        color: var(--text);
      }
      .container { max-width: 980px; margin: 0 auto; padding: 2rem 1rem 4rem; }
      .hero {
        text-align: center;
        padding: 3.5rem 1rem 2rem;
      }
      h1 { font-size: clamp(2rem, 6vw, 3.6rem); margin: 0 0 0.8rem; }
      p { color: var(--muted); line-height: 1.6; }
      .lead { max-width: 720px; margin: 0 auto 1.5rem; }
      .actions { display: flex; gap: 0.8rem; justify-content: center; flex-wrap: wrap; }
      .btn {
        text-decoration: none;
        padding: 0.75rem 1.1rem;
        border-radius: 12px;
        font-weight: 600;
      }
      .btn.primary { background: var(--accent); color: #0b1020; }
      .btn.ghost { border: 1px solid #36467f; color: var(--text); }
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 1rem;
        margin-top: 2rem;
      }
      .card {
        background: color-mix(in oklab, var(--card), black 8%);
        border: 1px solid #273362;
        border-radius: 14px;
        padding: 1rem;
      }
      h2 { margin-top: 0; font-size: 1.05rem; }
      .section { margin-top: 2.2rem; }
      footer {
        margin-top: 3rem;
        color: #98a6d8;
        font-size: 0.95rem;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <main class="container">
      <section class="hero">
        <p style="margin:0;opacity:.9;">Momentum for macOS</p>
        <h1>Track your real project time, not guesses.</h1>
        <p class="lead">
          Momentum helps you log focused work sessions in seconds, organize by project,
          and review where your time actually goes. Plus, log instantly with the Raycast extension.
        </p>
        <div class="actions">
          <a class="btn primary" href="#">Get early access</a>
          <a class="btn ghost" href="#">See how it works</a>
        </div>
      </section>

      <section class="grid section">
        <article class="card">
          <h2>Fast capture</h2>
          <p>Start/stop logs quickly so tracking never interrupts your flow.</p>
        </article>
        <article class="card">
          <h2>Project-first</h2>
          <p>Group sessions by project to understand effort and priorities clearly.</p>
        </article>
        <article class="card">
          <h2>Raycast extension</h2>
          <p>Capture time from anywhere with quick commands, no context switching.</p>
        </article>
      </section>

      <section class="section card">
        <h2>Built for focused sprints</h2>
        <p>
          Designed for developers and makers who work in intense, short cycles. Keep the data,
          cut the friction, and ship with confidence.
        </p>
      </section>

      <footer>
        Built by Miguel · Momentum
      </footer>
    </main>
  </body>
</html>
